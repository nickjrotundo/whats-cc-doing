# Example output

Generated by `ccdoing tick` running against this repository itself on
2026-08-30. status.json is the machine interface; status.html is its
human rendering (screenshot: ../docs/status-example.png).

Two things worth noticing in this capture:

- The `claude sessions` signal found the Claude Code session that was
  building this very tool - reading WORKING, named `portfolio-02`,
  process alive, with its active subagents listed - even though that
  session was started in a *parent* directory. Sessions are matched by
  the working directory their transcripts record, not just where they
  were launched (an earlier capture missed this session entirely; that
  bug is why the matching works this way now).
- The `title` ("whats-cc-doing dev") comes from `ccdoing.yaml`; omit it
  and the page falls back to plain "What's CC Doing".

If a signal here ever showed `no-match` or `stale`, that would be the
config-drift detection at work - the cue to run `ccdoing doctor --drift`
or the `/ccdoing:tune` skill.
